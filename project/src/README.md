# Исходный код

- `api/` — FastAPI сервис (`main.py`, `schemas.py`)
- `data/` — загрузка и чанкинг (`loader.py`, `chunker.py`)
- `retrieval/` — интерфейсы и реализации поиска (`base.py`, `bm25.py`, `dense.py`)
- `utils/` — метрики и утилиты